'use strict';

// Configuring the Articles module
angular.module('omims').run(['Menus',
	function(Menus) {
//		// Set top bar menu items
//		Menus.addMenuItem('topbar', 'Omims', 'omims', 'dropdown', '/omims(/create)?');
//		Menus.addSubMenuItem('topbar', 'omims', 'List Omims', 'omims');
//		Menus.addSubMenuItem('topbar', 'omims', 'New Omim', 'omims/create');
	}
]);