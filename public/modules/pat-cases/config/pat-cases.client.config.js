'use strict';

// Configuring the Articles module
angular.module('pat-cases').run(['Menus',
	function(Menus) {
		// Set top bar menu items
//		Menus.addMenuItem('topbar', 'Pat cases', 'pat-cases', 'dropdown', '/pat-cases(/create)?');
//		Menus.addSubMenuItem('topbar', 'pat-cases', 'List Pat cases', 'pat-cases');
//		Menus.addSubMenuItem('topbar', 'pat-cases', 'New Pat case', 'pat-cases/create');
	}
]);